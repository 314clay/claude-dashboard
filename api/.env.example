# SQLite Database (auto-created, usually no config needed)
# DB_PATH=~/.config/dashboard-native/dashboard.db

# AI Features (optional - set ONE key to enable)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...

# Advanced: Force a specific provider or model
# LLM_PROVIDER=openai            # Force provider: openai, anthropic, google, litellm
# LLM_MODEL=gpt-4o               # Override the default model for any provider

# LiteLLM proxy (legacy, backwards compatible)
# LITELLM_BASE_URL=http://localhost:4001
# LITELLM_API_KEY=sk-your-key-here

# Obsidian vault path (optional - for linking notes to sessions)
# OBSIDIAN_VAULT=/path/to/your/vault

# Path Configuration (optional - defaults to current user's home)
# USER_HOME=/home/youruser

# Project directories to scan (comma-separated, relative to USER_HOME)
# PROJECT_DIRS=Documents/GitHub,Projects,code
