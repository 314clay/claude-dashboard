# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_NAME=connectingservices

# Python Environment (optional - used by start.sh)
# Path to ConnectingServices venv for shared dependencies
# CONNECT_VENV=/path/to/connect/venv

# Path Configuration (optional - defaults to current user's home)
# USER_HOME=/home/youruser

# Project directories to scan (comma-separated, relative to USER_HOME)
# PROJECT_DIRS=w,Documents/GitHub,Projects,code

# LLM Configuration (for AI summaries and importance scoring)
# Set ONE of the following API keys. The first one found is used.
# If none are set, AI features (summaries, importance scoring) are disabled.

# Option 1: OpenAI (default model: gpt-4o-mini)
# OPENAI_API_KEY=sk-your-openai-key

# Option 2: Anthropic (default model: claude-sonnet-4-5-20250929)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Option 3: Google Gemini (default model: gemini-2.5-flash)
# GOOGLE_API_KEY=your-google-api-key

# Option 4: LiteLLM proxy (backwards compatible)
# LITELLM_BASE_URL=http://localhost:4001
# LITELLM_API_KEY=sk-your-key-here

# Advanced: Force a specific provider or model
# LLM_PROVIDER=openai            # Force provider: openai, anthropic, google, litellm
# LLM_MODEL=gpt-4o               # Override the default model for any provider

# Obsidian vault path (optional - for linking notes to sessions)
# OBSIDIAN_VAULT=/path/to/your/vault
