# Database Configuration
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_NAME=connectingservices

# Path Configuration (optional - defaults to current user's home)
# USER_HOME=/home/youruser

# Project directories to scan (comma-separated, relative to USER_HOME)
# PROJECT_DIRS=w,Documents/GitHub,Projects,code

# LLM Configuration (for AI summaries and importance scoring)
# Set up a LiteLLM proxy or use direct API access
LITELLM_BASE_URL=http://localhost:4001
LITELLM_API_KEY=sk-your-key-here

# Alternative: Use Gemini directly (requires google-generativeai package)
# GOOGLE_API_KEY=your-gemini-api-key

# Model to use for summaries (default: gemini-2.5-flash)
# SUMMARY_MODEL=gemini-2.5-flash

# Obsidian vault path (optional - for linking notes to sessions)
# OBSIDIAN_VAULT=/path/to/your/vault
